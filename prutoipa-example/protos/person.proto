syntax = "proto3";

package people;

enum GENDER {
    MALE = 0;
    FEMALE = 1;
}

message Person {
    int32 id = 1;
    repeated int64 neighbours = 2;
    string name = 3;
    optional string surname = 4;
}